(function(){'use strict';
function released(form){var radio=form.querySelector('input[name="trb_release_state"]:checked');return radio&&radio.value==='previously_released';}
function normalize(value){return String(value||'').toUpperCase().replace(/[^A-Z0-9]/g,'');}
function tracks(form){return Array.prototype.slice.call(form.querySelectorAll('[data-track]'));}
function sync(form){var active=released(form);tracks(form).forEach(function(track,index){var holder=track.querySelector('[data-trb-isrc-field]');if(!holder){holder=document.createElement('label');holder.className='trb-release-isrc-field';holder.setAttribute('data-trb-isrc-field','');holder.innerHTML='ISRC già assegnato <span aria-hidden="true">*</span><input type="text" inputmode="latin" autocomplete="off" maxlength="15" placeholder="ITV242600005" /><small>Inserisci il codice della registrazione già pubblicata. Trattini e spazi vengono rimossi automaticamente.</small>';var grid=track.querySelector('.trb-portal__field-grid');if(grid)grid.appendChild(holder);holder.querySelector('input').addEventListener('input',function(){var input=this,clean=normalize(input.value);if(input.value!==clean)input.value=clean;input.setCustomValidity(clean&& !/^[A-Z]{2}[A-Z0-9]{3}[0-9]{7}$/.test(clean)?'Inserisci un ISRC valido di 12 caratteri.':'');});}
var input=holder.querySelector('input');holder.hidden=!active;input.disabled=!active;input.required=active;input.name='trb_existing_isrc['+index+']';if(!active){input.setCustomValidity('');}else{input.dispatchEvent(new Event('input'));}});}
function addReviewIsrc(form){var modal=document.querySelector('.trb-release-review');if(!modal||modal.hidden)return;var sections=modal.querySelectorAll('.trb-release-review__section');tracks(form).forEach(function(track,index){var input=track.querySelector('[data-trb-isrc-field] input');if(!input||input.disabled||!sections[index+1]||sections[index+1].querySelector('[data-review-isrc]'))return;var row=document.createElement('div');row.className='trb-release-review__row';row.setAttribute('data-review-isrc','');var dt=document.createElement('dt');dt.textContent='ISRC';var dd=document.createElement('dd');dd.textContent=input.value;row.appendChild(dt);row.appendChild(dd);sections[index+1].querySelector('dl').appendChild(row);});}
document.addEventListener('DOMContentLoaded',function(){var form=document.querySelector('[data-release-form]');if(!form)return;sync(form);form.addEventListener('change',function(event){if(event.target.matches('input[name="trb_release_state"]'))sync(form);});var observer=new MutationObserver(function(){sync(form);addReviewIsrc(form);});observer.observe(form,{childList:true,subtree:true});var bodyObserver=new MutationObserver(function(){addReviewIsrc(form);});bodyObserver.observe(document.body,{childList:true,subtree:true,attributes:true,attributeFilter:['hidden']});form.addEventListener('submit',function(){sync(form);},true);});
}());
